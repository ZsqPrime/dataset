
Tool list, put these in your path:

git-distclean

  Inspired by 'make distclean' and that 'git clean -f -d -x' cleans a bit too much (like IDE project files).

    git distclean help

    for more details like configuration, etc.



Project Description:
Misc tools that I've created

README:

Tool list, put these in your path:

git-distclean

  Inspired by 'make distclean' and that 'git clean -f -d -x' cleans a bit too much (like IDE project files).

    git distclean help

    for more details like configuration, etc.