# Loadbal

### sets / unsets load balancing policy on router: TP-Link, model: TL-R470T+

### usage:

from shell:

    loadbal activate
    loadbal deactivate

Project Description:
sets / unsets load balancing policy on TP-Link router [mechanize]

README:
# Loadbal

### sets / unsets load balancing policy on router: TP-Link, model: TL-R470T+

### usage:

from shell:

    loadbal activate
    loadbal deactivate