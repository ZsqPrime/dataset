LibGBasm
-Overview-
This library allows one to fetch, from an offset in a binary, a formatted
instruction, containing the arguments in their various formats.

-Usage-
In your project, make sure to import "LibGBasm". From there, any function
available will be through the LibGBasm namespace.