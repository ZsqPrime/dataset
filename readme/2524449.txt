Project Description:
Python module to work with bounded data. It's inspired in Pascal bounds checking

README:
bounds is a Python module that add support for bounded types inspired in Pascal bounded checkers. This would allow for example to get an error when trying to use a number outside a predefined range, or use this range to iterate over it being defined only one time.