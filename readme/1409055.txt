#ACE DSSAT Import

This tool is used to import information into an ACE (AgMIP Crop
Experiment) formatted database from DSSAT files.

##Configuration

* Modify the `dcConnect_sample.php` and rename it to `dbConnect.db`


Project Description:
AgMIP tool to import DSSAT information into an ACE (AgMIP Crop Experiment) Database

README:
#ACE DSSAT Import

This tool is used to import information into an ACE (AgMIP Crop
Experiment) formatted database from DSSAT files.

##Configuration

* Modify the `dcConnect_sample.php` and rename it to `dbConnect.db`